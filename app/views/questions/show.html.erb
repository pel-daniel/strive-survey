<h2><%= @question.text %></h2>

<%= form_for [@survey_response, @question, @answer] do |f| %>
  <div>
    <%= f.text_area :text, autofocus: true %>
  </div>

  <div>
    <%= f.submit 'Next' %>
  </div>
<% end %>
